# Database Schema

This template repository does not currently define an application database schema.

If a future project adds a database, generate or document the schema here so agents can inspect tables, relationships, constraints, and migration state without querying production systems.
