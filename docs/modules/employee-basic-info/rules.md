# Employee Basic Info Rules

- Reuse the existing employee aggregate and persistence path.
- Do not add database schema changes for the basic-info example.
- Keep the request shape limited to `EmployeeCode`, `FullName`, and `Email`.
- Keep default profile fields explicit in the use case.
- Cover both use-case behavior and API persistence in tests.
